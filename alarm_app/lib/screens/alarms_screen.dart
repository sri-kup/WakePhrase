import 'package:flutter/material.dart';

class AlarmsScreen extends StatelessWidget {
  const AlarmsScreen({Key? key}) : super(key: key); // Add Key parameter

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Alarms Tab'),
    );
  }
}
